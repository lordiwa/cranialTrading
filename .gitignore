# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/settings.json
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

# Playwright
e2e/.auth/
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
nightly-results.json

# Claude Code - ephemeral/local artifacts
.claude/worktrees/
.claude/plans/
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Build/tool artifacts
graphify-out/
eslint-out.json
NUL
.firebase/hosting.*.cache

# Retired tools
testsprite_tests/

# Unused public mockups/screenshots
public/*.png
public/*.jpeg

# fixture de divergencia de card_index (scripts/card-index-fixture.mjs)
.card-index-fixture.json

# estado de cursor del backfill de chunkId (scripts/backfill-card-chunk-id.mjs,
# TASK-230) — progreso de una corrida local por-maquina, no aporta nada versionado.
# El nombre incluye el projectId (dev/prod nunca comparten archivo — HIGH-1).
.backfill-card-chunk-id-state.*.json

# Sub-proyecto de diseño: va a su PROPIO repositorio (TASK-225).
# OJO: hoy vive SOLO en el disco local, sin copia en ningun remoto.
cranial-design/

# Foto del tablero kanban que quedo bajo public/ (TASK-224).
# Nada interno debe vivir en public/: Vite lo copiaria al sitio publicado.
public/tasks/
