---
phase: 04-globalsearch-navigation-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/components/ui/GlobalSearch.vue
  - src/components/ui/MobileSearchOverlay.vue
  - src/components/ui/CardFilterBar.vue
  - src/components/layout/AppHeader.vue
  - src/components/layout/MatchNotificationsDropdown.vue
  - src/components/collection/AddCardModal.vue
  - src/views/CollectionView.vue
  - src/views/DeckView.vue
  - src/views/BinderView.vue
  - src/locales/en.json
  - src/locales/es.json
  - src/locales/pt.json
autonomous: false
requirements:
  - NICE-11
  - ARCH-10
requirements_addressed:
  - NICE-11
  - ARCH-10
must_haves:
  truths:
    - "Cmd+click on a GlobalSearch collection result opens /collection?search=<name> in a new tab"
    - "Cmd+click on a GlobalSearch user result opens /@<username> in a new tab"
    - "Cmd+click on a GlobalSearch scryfall result opens /collection?addCard=<name> in a new tab"
    - "Cmd+click on the FAQ menu item in AppHeader opens /faq in a new tab"
    - "Cmd+click on a match notification in MatchNotificationsDropdown opens /saved-matches?match=<id> in a new tab"
    - "Cmd+click on the 'Advanced search' link in GlobalSearch, MobileSearchOverlay, AddCardModal, and the CardFilterBar suggestions dropdown opens /search in a new tab"
    - "Normal left-click on any converted link navigates via Vue Router AND runs the side effect (close dropdown / clear query / emit close)"
    - "All new i18n keys exist in en.json, es.json, and pt.json"
    - "MobileSearchOverlay uses header.search.clearAriaLabel (not the legacy common.aria.clearInput)"
    - "Custom useI18n pluralization is honored: two keys + ternary at call site, no pipe/tc usage"
    - "Dead handleOpenAdvancedSearch handlers in CollectionView.vue, DeckView.vue, and BinderView.vue are removed after CardFilterBar RouterLink conversion supersedes the open-advanced-search emit (D-07 parent-view cleanup)"
  artifacts:
    - path: "src/locales/en.json"
      provides: "New header.search.clearAriaLabel, searching, resultsCount, resultsCountSingular, tabNames.{collection,users,scryfall} keys"
      contains: ["header.search.clearAriaLabel", "header.search.resultsCount", "header.search.tabNames"]
    - path: "src/locales/es.json"
      provides: "Spanish translations for the same 7 new keys"
      contains: ["header.search.clearAriaLabel", "header.search.resultsCount", "header.search.tabNames"]
    - path: "src/locales/pt.json"
      provides: "Portuguese translations for the same 7 new keys"
      contains: ["header.search.clearAriaLabel", "header.search.resultsCount", "header.search.tabNames"]
  key_links:
    - from: "Result rows in GlobalSearch.vue / MobileSearchOverlay.vue"
      to: "/collection, /@username, /collection?addCard=... routes"
      via: "<RouterLink :to=\"resolve*Route(card)\">"
      pattern: "RouterLink.*resolve(Collection|User|Scryfall)Route"
    - from: "FAQ button in AppHeader.vue"
      to: "/faq route"
      via: "<RouterLink :to=\"'/faq'\" @click=\"closeHelpMenu(); closeMobileMenu()\">"
      pattern: "RouterLink.*to=.*\\/faq"
    - from: "Match notifications in MatchNotificationsDropdown.vue"
      to: "/saved-matches route"
      via: "<RouterLink :to=\"...\" @click=\"closeDropdown()\">"
      pattern: "RouterLink.*closeDropdown"
    - from: "CardFilterBar.vue advanced-search suggestion"
      to: "/search route (direct — no parent emit)"
      via: "<RouterLink :to=\"'/search'\" @click=\"clearSuggestions(); dropdownDismissed = true\">"
      pattern: "RouterLink.*'/search'"
    - from: "Parent views (CollectionView.vue:693, DeckView.vue:283, BinderView.vue:155)"
      to: "dead handler removal post-CardFilterBar-conversion"
      via: "grep removes handleOpenAdvancedSearch + its event binding after CardFilterBar emits no longer fire"
      pattern: "handleOpenAdvancedSearch"
---

<objective>
Convert user-triggered navigation button clicks to `<RouterLink>` elements so Cmd+click, Ctrl+click, and middle-click open destinations in new tabs (NICE-11). Add the i18n keys referenced by Plan 02's template changes to all three locale files (Anti-loop Rule 3). Rewire GlobalSearch and MobileSearchOverlay result rows to use the route resolvers from Plan 01.

Purpose: NICE-11 closes the Phase 4 user-facing requirement. The ARIA combobox (ARCH-10) from Plans 01-02 becomes fully production-ready once the i18n keys exist and the result rows support modifier-click semantics. This plan is the observable endpoint: without it, Plan 02's template bindings still reference undefined i18n keys that fall back to the key strings, and the combobox result navigation does not honor Cmd+click.

Output:
- 15 template-level RouterLink conversions across 8 files
- 7 new i18n keys in 3 locale files (21 additions total)
- MobileSearchOverlay.vue migrated from `common.aria.clearInput` to `header.search.clearAriaLabel`
- Dead code cleanup in CardFilterBar.vue AND the three parent views (CollectionView.vue:693, DeckView.vue:283, BinderView.vue:155) — `handleOpenAdvancedSearch` handlers + `@open-advanced-search` bindings removed after the CardFilterBar RouterLink supersedes the emit pattern (per D-07)
- Dead-code cleanup: if `goToAdvancedSearch` wrapper in MobileSearchOverlay becomes unused, remove it

Explicitly OUT of scope (per D-07 corrected + D-08 — Q8 classification):
- `SearchView.vue:38` — rendered as `<BaseButton>` not anchor; converting requires BaseButton API change; stays as `router.push`
- `DecksView.vue:331/388/487/552` — imperative redirects after await (post-create/post-import); stay as `router.push`
- `DecksView.vue:335` `handleEditDeck` — called from `@edit` emit of DeckCard component whose own internal click handler is a `<div @click>`, not a button; converting would require DeckCard API change (out of Phase 4 scope); stays as `router.push`
- `CollectionView.vue:627/629/659` — post-action redirects; stay as `router.push`
- `UserPopover.vue:221` — post-await-logout redirect; stays as `router.push`
- `AppHeader.vue:57` (restartTour) — imperative navigate-then-start-tour flow; stays as `router.push`
- `AppHeader.vue:167/169` — modal-open intents (addCard query param), not destination nav; stay as `router.push`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-globalsearch-navigation-polish/04-CONTEXT.md
@.planning/phases/04-globalsearch-navigation-polish/04-RESEARCH.md
@.planning/phases/04-globalsearch-navigation-polish/04-01-PLAN.md
@.planning/phases/04-globalsearch-navigation-polish/04-02-PLAN.md
@CLAUDE.md
@src/composables/useGlobalSearch.ts
@src/components/ui/GlobalSearch.vue
@src/components/ui/MobileSearchOverlay.vue
@src/components/ui/CardFilterBar.vue
@src/components/layout/AppHeader.vue
@src/components/layout/MatchNotificationsDropdown.vue
@src/components/collection/AddCardModal.vue
@src/views/CollectionView.vue
@src/views/DeckView.vue
@src/views/BinderView.vue
@src/locales/en.json
@src/locales/es.json
@src/locales/pt.json

<interfaces>
<!-- Route resolvers from Plan 01 available for :to= binding: -->
```typescript
resolveCollectionRoute(card: Card) → { path: '/collection', query: { search: card.name } }
resolveUserRoute(card: PublicCardResult) → `/@${card.username ?? ''}`
resolveScryfallRoute(card: ScryfallCard) → { path: '/collection', query: { addCard: card.name } }
```

<!-- Existing Vue Router usage patterns in codebase (grep confirmed 185+ RouterLink sites): -->
```html
<!-- Simple target: -->
<RouterLink :to="'/some-path'">...</RouterLink>
<RouterLink to="/terms" @click="closeHelpMenu">...</RouterLink>

<!-- Dynamic target with query: -->
<RouterLink :to="{ path: '/collection', query: { search: card.name } }">...</RouterLink>

<!-- Dynamic with side effect (CORRECT pattern — NO .prevent): -->
<RouterLink :to="route" @click="sideEffect()">...</RouterLink>
```

<!-- Existing header.search keys in en.json (lines 349-360): -->
```json
"header": {
  "search": {
    "placeholder": "Search cards…",
    "tabs": {
      "collection": "My collection",
      "users": "Other users",
      "scryfall": "Scryfall"
    },
    "noResults": "No results",
    "addToCollection": "Add",
    "cancel": "Cancel",
    "advancedSearch": "Advanced Search"
  }
}
```

<!-- D-09 CORRECTION (verbatim from 04-RESEARCH.md Q3):
     DO NOT use @click.prevent on RouterLink. Vue fires the handler first (sets e.defaultPrevented),
     then RouterLink's guardEvent sees defaultPrevented=true and SKIPS router.push entirely.
     Result: normal left-clicks would not navigate. Use @click (without .prevent) for side effects. -->

<!-- D-07 parent-view cleanup targets (CONFIRMED by direct file read 2026-04-17): -->
```typescript
// src/views/CollectionView.vue:691-694
// Click on "Advanced search" → navigate to /search
const handleOpenAdvancedSearch = () => {
  void router.push({ path: '/search' })
}

// src/views/DeckView.vue:282-284
const handleOpenAdvancedSearch = () => {
  void router.push({ path: '/search' })
}

// src/views/BinderView.vue:154-156
const handleOpenAdvancedSearch = () => {
  void router.push({ path: '/search' })
}
```
Each is bound in the template via `@open-advanced-search="handleOpenAdvancedSearch"` on a `<CardFilterBar ...>` element. After Task 3 converts CardFilterBar's internal button to a RouterLink, the emit is gone → these parent handlers become dead code and MUST be removed in the same task (Anti-loop Rule 6 — parallel siblings).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7 new i18n keys to all three locale files (en.json, es.json, pt.json)</name>
  <files>src/locales/en.json, src/locales/es.json, src/locales/pt.json</files>
  <read_first>
    - src/locales/en.json (lines 349-361 — existing header.search block)
    - src/locales/es.json (find header.search block — same path, Spanish values)
    - src/locales/pt.json (find header.search block — same path, Portuguese values)
    - .planning/phases/04-globalsearch-navigation-polish/04-CONTEXT.md (D-19 corrected — exact key names and translation strings)
    - .planning/phases/04-globalsearch-navigation-polish/04-RESEARCH.md (Q4 — two-key pattern confirmed)
    - CLAUDE.md (Anti-loop Rule 3 — all 3 locale files in the same task, always)
  </read_first>
  <action>
Extend the `header.search` block in ALL THREE locale files SIMULTANEOUSLY with these 7 keys. Anti-loop Rule 3: no partial file edits.

**en.json — extend header.search (preserve existing keys):**
```json
"header": {
  ...other keys...
  "search": {
    "placeholder": "Search cards…",
    "tabs": {
      "collection": "My collection",
      "users": "Other users",
      "scryfall": "Scryfall"
    },
    "noResults": "No results",
    "addToCollection": "Add",
    "cancel": "Cancel",
    "advancedSearch": "Advanced Search",
    "clearAriaLabel": "Clear search",
    "searching": "Searching…",
    "resultsCount": "{count} results in {tabName}",
    "resultsCountSingular": "{count} result in {tabName}",
    "tabNames": {
      "collection": "collection",
      "users": "users",
      "scryfall": "Scryfall"
    }
  }
}
```

**es.json — same keys, Spanish values:**
```json
"clearAriaLabel": "Limpiar búsqueda",
"searching": "Buscando…",
"resultsCount": "{count} resultados en {tabName}",
"resultsCountSingular": "{count} resultado en {tabName}",
"tabNames": {
  "collection": "colección",
  "users": "usuarios",
  "scryfall": "Scryfall"
}
```

**pt.json — same keys, Portuguese values:**
```json
"clearAriaLabel": "Limpar busca",
"searching": "Buscando…",
"resultsCount": "{count} resultados em {tabName}",
"resultsCountSingular": "{count} resultado em {tabName}",
"tabNames": {
  "collection": "coleção",
  "users": "usuários",
  "scryfall": "Scryfall"
}
```

**Important constraints:**
- DO NOT delete the existing `common.aria.clearInput` key. Other components may still use it. This task only adds new keys.
- Do NOT add `{count} item | {count} items` pipe-plural syntax anywhere. Per D-19 corrected, this project's custom useI18n does not parse the pipe.
- JSON must remain valid — verify with `node -e "JSON.parse(require('fs').readFileSync('src/locales/en.json'))"` (and same for es/pt) after editing.
- Preserve existing indentation and key ordering style of each file.

After editing:
- Run `npm run test:unit` — no regressions expected (locale JSON changes don't affect tests).
- Run `npx vite build` — must exit 0.

Commit: `i18n(04-03): add header.search ARIA keys to en/es/pt`
  </action>
  <verify>
    <automated>grep -c "header.search.clearAriaLabel\|\"clearAriaLabel\"" src/locales/en.json src/locales/es.json src/locales/pt.json && grep -c "resultsCount" src/locales/en.json src/locales/es.json src/locales/pt.json && grep -c "tabNames" src/locales/en.json src/locales/es.json src/locales/pt.json && node -e "JSON.parse(require('fs').readFileSync('src/locales/en.json')); JSON.parse(require('fs').readFileSync('src/locales/es.json')); JSON.parse(require('fs').readFileSync('src/locales/pt.json')); console.log('valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\"clearAriaLabel\"" src/locales/en.json` returns exactly 1 match
    - `grep "\"clearAriaLabel\"" src/locales/es.json` returns exactly 1 match
    - `grep "\"clearAriaLabel\"" src/locales/pt.json` returns exactly 1 match
    - `grep "\"searching\"" src/locales/en.json src/locales/es.json src/locales/pt.json` returns at least 3 matches
    - `grep "\"resultsCount\"" src/locales/en.json src/locales/es.json src/locales/pt.json` returns at least 3 matches
    - `grep "\"resultsCountSingular\"" src/locales/en.json src/locales/es.json src/locales/pt.json` returns at least 3 matches
    - `grep "\"tabNames\"" src/locales/en.json src/locales/es.json src/locales/pt.json` returns at least 3 matches
    - `grep "{count} item | {count} items" src/locales/en.json` returns 0 or 1 (existing dormant key may stay; do NOT add this pattern for new keys)
    - `grep "resultsCount.*|" src/locales/en.json src/locales/es.json src/locales/pt.json` returns 0 matches (no pipe pluralization in NEW keys)
    - All three locale files still parse as valid JSON (Node JSON.parse succeeds)
    - `grep "common.aria.clearInput" src/locales/en.json` returns at least 1 match (OLD key preserved, other components may use it)
    - `npx vite build` exits 0
    - Git commit exists with message matching `i18n(04-03): add header.search ARIA keys`
  </acceptance_criteria>
  <done>Seven new keys present in all three locale files with proper translations. Existing keys untouched. JSON valid. Build succeeds. Single atomic commit per Rule 3.</done>
</task>

<task type="auto">
  <name>Task 2: Convert GlobalSearch.vue + MobileSearchOverlay.vue result rows + advanced-search links to RouterLink (parallel siblings)</name>
  <files>src/components/ui/GlobalSearch.vue, src/components/ui/MobileSearchOverlay.vue</files>
  <read_first>
    - src/components/ui/GlobalSearch.vue (current state after Plan 02 ARIA changes)
    - src/components/ui/MobileSearchOverlay.vue (current state after Plan 02 ARIA changes)
    - src/composables/useGlobalSearch.ts (confirm resolveCollectionRoute/resolveUserRoute/resolveScryfallRoute are exported)
    - .planning/phases/04-globalsearch-navigation-polish/04-CONTEXT.md (D-07, D-09 corrected, D-10, D-11, D-21)
    - .planning/phases/04-globalsearch-navigation-polish/04-RESEARCH.md (Q3 modifier-click behavior, Pitfall 2 — NO `.prevent` on RouterLink with `:to=`)
    - CLAUDE.md (Anti-loop Rule 6 — both files in same task)
  </read_first>
  <action>
**Edit BOTH files in this single task per Anti-loop Rule 6.** Apply each change to GlobalSearch.vue first, then immediately mirror to MobileSearchOverlay.vue before moving on.

**STEP 1 — Update script destructure in GlobalSearch.vue.**

Extend the `useGlobalSearch()` destructure to include route resolvers:
```typescript
const {
  ...existing...
  activeDescendantId, ariaLiveMessage, isExpanded,
  moveHighlight, selectHighlighted,
  // NEW in Task 2:
  resolveCollectionRoute, resolveUserRoute, resolveScryfallRoute,
} = useGlobalSearch()
```

The `router` import (line 10 currently `import { useRouter } from 'vue-router'` + `const router = useRouter()`) — keep it. The advanced-search button below still uses `router.push` directly in the current template (line 107); we'll convert that too.

But also import `RouterLink`:
```typescript
import { RouterLink, useRouter } from 'vue-router'
```

**STEP 2 — Convert the Advanced Search button in GlobalSearch.vue (currently line 106-111).**

Replace:
```html
<button
    @click="isOpen = false; router.push('/search')"
    class="text-tiny text-silver-50 hover:text-neon transition-colors"
>
  {{ t('common.actions.advancedSearch') }} →
</button>
```

With (note: NO `.prevent`):
```html
<RouterLink
    :to="'/search'"
    @click="isOpen = false"
    class="text-tiny text-silver-50 hover:text-neon transition-colors"
>
  {{ t('common.actions.advancedSearch') }} →
</RouterLink>
```

**STEP 3 — Convert the three result-row buttons in GlobalSearch.vue.**

Current collection row (approx lines 162-182 after Plan 02's ARIA changes) is a `<button v-for="(card, index) in collectionResults" :id=... role="option" ... @click="goToCollection(card)">`. Replace with:
```html
<RouterLink
  v-for="(card, index) in collectionResults"
  :key="card.id"
  :id="`option-collection-${index}`"
  role="option"
  :aria-selected="activeDescendantId === `option-collection-${index}` ? 'true' : 'false'"
  :to="resolveCollectionRoute(card)"
  @click="isOpen = false; searchQuery = ''"
  class="w-full px-4 py-3 flex items-center gap-3 hover:bg-silver-10 transition-colors text-left border-b border-silver-20 last:border-0"
>
  ...existing inner content (img + text + price)...
</RouterLink>
```

Users row — trickier. Current structure (post-Plan-02):
- Outer `<div role="option" :id="..." :aria-selected="..." class="...flex...">` (was already a div per D-10)
- Inner `<button class="flex-1..." @click="goToUserCard(card)">` for card select
- Sibling ME INTERESA `<button>` / sent-span

D-10 says ME INTERESA stays OUTSIDE any anchor. The card-select button is INSIDE the option div. Convert the inner button to a `<RouterLink>`:

```html
<div
  v-for="(card, index) in usersResults"
  :key="card.id"
  :id="`option-users-${index}`"
  role="option"
  :aria-selected="activeDescendantId === `option-users-${index}` ? 'true' : 'false'"
  class="px-4 py-3 flex items-center gap-3 hover:bg-silver-10 transition-colors border-b border-silver-20 last:border-0"
>
  <RouterLink
    :to="resolveUserRoute(card)"
    @click="isOpen = false; searchQuery = ''"
    class="flex-1 min-w-0 flex items-center gap-3 text-left focus-visible:ring-2 focus-visible:ring-neon focus-visible:outline-none rounded"
  >
    ...existing img + user info content...
  </RouterLink>
  <div class="flex flex-col items-end gap-1 flex-shrink-0">
    <span class="text-tiny text-neon font-bold">${{ card.price?.toFixed(2) ?? 'N/A' }}</span>
    <button
      v-if="!sentInterestIds.has(card.id)"
      @click.stop="sendInterestFromSearch(card)"
      ...existing ME INTERESA button...
    >
      ME INTERESA
    </button>
    ...existing sent-span...
  </div>
</div>
```

**Nesting verification:** The ME INTERESA button is a sibling of RouterLink inside the outer div, NOT nested inside RouterLink. This satisfies D-10 and avoids the anti-pattern "nested interactive elements in an `<a>`".

Scryfall row (current post-Plan-02 is a `<button ... @click="goToScryfall(card)">`):
```html
<RouterLink
  v-for="(card, index) in scryfallResults"
  :key="card.id"
  :id="`option-scryfall-${index}`"
  role="option"
  :aria-selected="activeDescendantId === `option-scryfall-${index}` ? 'true' : 'false'"
  :to="resolveScryfallRoute(card)"
  @click="isOpen = false; searchQuery = ''"
  class="w-full px-4 py-3 flex items-center gap-3 hover:bg-silver-10 transition-colors text-left border-b border-silver-20 last:border-0"
>
  ...existing img + name + mana cost + '+ Add' content...
</RouterLink>
```

**IMPORTANT (D-09 correction):** Use `@click="isOpen = false; searchQuery = ''"` — NO `.prevent`. Verified from Vue Router source: `.prevent` sets `e.defaultPrevented=true` BEFORE `guardEvent` runs, which would cause RouterLink to skip navigation on normal clicks.

**STEP 4 — Mirror ALL of Step 3 to MobileSearchOverlay.vue.**

The mobile overlay has a slightly different structure but parallel changes:
- Import `RouterLink` from vue-router
- Extend destructure with `resolveCollectionRoute, resolveUserRoute, resolveScryfallRoute`
- Convert collection/users/scryfall row `<button>` (or outer `<div>` for users) to RouterLink
- For `@click` side effects, use the existing `emit('close')` pattern: `@click="emit('close')"` — just remove `.prevent` if present. The existing MobileSearchOverlay code uses wrapper functions `goToCollection/goToUserCard/goToScryfall` that call the underlying composable function AND `emit('close')`. After conversion, those wrappers become unused IF the RouterLink handles navigation and `emit('close')` handles the side effect.

Post-conversion pattern for mobile (collection row example):
```html
<RouterLink
  v-for="(card, index) in collectionResults"
  :key="card.id"
  :id="`option-collection-${index}`"
  role="option"
  :aria-selected="activeDescendantId === `option-collection-${index}` ? 'true' : 'false'"
  :to="resolveCollectionRoute(card)"
  @click="emit('close')"
  class="w-full px-4 py-3 flex items-center gap-3 hover:bg-silver-10 active:bg-silver-10 transition-colors text-left border-b border-silver-20 last:border-0"
>
  ...existing content...
</RouterLink>
```

Similarly for users and scryfall rows.

**STEP 5 — Convert the "Advanced Search" button at the bottom of MobileSearchOverlay (line 301-306 in pre-Plan-02 code).**

Current:
```html
<button
  @click="goToAdvancedSearch"
  class="w-full text-center text-small text-silver-50 hover:text-neon transition-colors"
>
  {{ t('header.search.advancedSearch') }} →
</button>
```

Replace with:
```html
<RouterLink
  :to="'/search'"
  @click="emit('close')"
  class="w-full block text-center text-small text-silver-50 hover:text-neon transition-colors"
>
  {{ t('header.search.advancedSearch') }} →
</RouterLink>
```

Note: added `block` class because RouterLink renders as `<a>` which is inline by default, while the previous `<button>` was block-ish via parent flex layout. Verify the bottom bar layout in the checkpoint.

**STEP 6 — Dead-code cleanup in MobileSearchOverlay.vue script.**

After all result-row conversions, check whether the local wrappers `goToCollection`, `goToUserCard`, `goToScryfall` (lines 64-77) are still referenced. They are:
- Used by template `@click` — REMOVED after conversion
- Used by `selectHighlighted` — NO (selectHighlighted is inside the composable and uses the composable's own goTo* + a direct emit('close') is not possible from inside the composable)

Important: after conversion, the Enter-key path in `selectHighlighted` (composable) still calls the composable's own `goToCollection/goToUserCard/goToScryfall`, which do `router.push` + close `isOpen`. For MobileSearchOverlay, pressing Enter should ALSO close the overlay. Check whether the composable's `isOpen = false` propagates to closing the mobile overlay. Reading the code:
- MobileSearchOverlay is controlled by its `props.open` prop from parent
- `clearSearch()` (from composable, called by composable's goTo*) sets `isOpen.value = false` — but that's the DESKTOP isOpen
- The mobile overlay has its OWN `open` prop that doesn't react to composable isOpen

**Therefore, MobileSearchOverlay's Enter-key path needs extra handling.** Update its `handleInputKeydown` (added in Plan 02) so that after Enter-selecting an option OR navigating to /search, it ALSO calls `emit('close')`:

```typescript
const handleInputKeydown = (e: KeyboardEvent) => {
  if (e.isComposing || e.keyCode === 229) return
  if (e.key === 'Escape') {
    close()
    return
  }
  if (!isExpanded.value) return
  switch (e.key) {
    case 'ArrowDown': e.preventDefault(); moveHighlight('down'); break
    case 'ArrowUp':   e.preventDefault(); moveHighlight('up'); break
    case 'Home':      e.preventDefault(); moveHighlight('home'); break
    case 'End':       e.preventDefault(); moveHighlight('end'); break
    case 'Enter':
      e.preventDefault()
      selectHighlighted()
      emit('close')   // NEW: ensure mobile overlay closes after Enter-selection
      break
  }
}
```

The existing local wrappers `goToCollection/goToUserCard/goToScryfall` (lines 64-77) + `goToAdvancedSearch` (line 79-82) can now be removed if nothing else references them. Grep first:
```bash
grep -n "goToCollection\|goToUserCard\|goToScryfall\|goToAdvancedSearch" src/components/ui/MobileSearchOverlay.vue
```
If the only remaining references are their own declarations + the destructured `_goToCollection/_goToUserCard/_goToScryfall` (which can also be removed from the destructure now), delete the dead code. Leave only the composable destructure without them.

Run `npm run test:unit`, `npx vite build` — both must pass.

Commit: `feat(04-03): convert GlobalSearch and MobileSearchOverlay results to RouterLink`
  </action>
  <verify>
    <automated>grep -c "RouterLink" src/components/ui/GlobalSearch.vue && grep -c "RouterLink" src/components/ui/MobileSearchOverlay.vue && grep "@click.prevent" src/components/ui/GlobalSearch.vue src/components/ui/MobileSearchOverlay.vue | grep -i "RouterLink" ; npm run test:unit && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<RouterLink" src/components/ui/GlobalSearch.vue` returns at least 4 (3 result-type RouterLinks + 1 advanced-search link)
    - `grep -c "<RouterLink" src/components/ui/MobileSearchOverlay.vue` returns at least 4 (3 result-type RouterLinks + 1 advanced-search link)
    - `grep "resolveCollectionRoute\|resolveUserRoute\|resolveScryfallRoute" src/components/ui/GlobalSearch.vue` returns at least 3 matches
    - `grep "resolveCollectionRoute\|resolveUserRoute\|resolveScryfallRoute" src/components/ui/MobileSearchOverlay.vue` returns at least 3 matches
    - `grep -B1 -A1 "@click.prevent" src/components/ui/GlobalSearch.vue | grep -i "RouterLink"` returns 0 matches (D-09 correction — no .prevent on RouterLinks)
    - `grep -B1 -A1 "@click.prevent" src/components/ui/MobileSearchOverlay.vue | grep -i "RouterLink"` returns 0 matches
    - `grep -E "RouterLink[^>]*@click\.prevent" src/components/ui/GlobalSearch.vue src/components/ui/MobileSearchOverlay.vue` multiline — 0 matches
    - `grep "ME INTERESA" src/components/ui/GlobalSearch.vue` and check surrounding context — the button is OUTSIDE any RouterLink (D-10)
    - `grep "import.*RouterLink.*vue-router" src/components/ui/GlobalSearch.vue src/components/ui/MobileSearchOverlay.vue` returns 2 matches (one per file)
    - `grep "common.aria.clearInput" src/components/ui/MobileSearchOverlay.vue` returns 0 matches (replaced with header.search.clearAriaLabel in Plan 02; verify carryover)
    - `grep "header.search.clearAriaLabel" src/components/ui/MobileSearchOverlay.vue` returns at least 1 match
    - `npm run test:unit` exits 0
    - `npx vite build` exits 0
    - Git commit exists with `feat(04-03): convert GlobalSearch and MobileSearchOverlay results to RouterLink`
  </acceptance_criteria>
  <done>Both GlobalSearch and MobileSearchOverlay use RouterLink for result rows and advanced-search links. No `.prevent` on any RouterLink. ME INTERESA button sibling structure preserved. Dead goTo* wrappers removed from mobile overlay if no longer referenced. Build + tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Convert remaining NICE-11 sites — AppHeader FAQ, MatchNotificationsDropdown, AddCardModal, CardFilterBar + D-07 parent-view dead-handler cleanup</name>
  <files>src/components/layout/AppHeader.vue, src/components/layout/MatchNotificationsDropdown.vue, src/components/collection/AddCardModal.vue, src/components/ui/CardFilterBar.vue, src/views/CollectionView.vue, src/views/DeckView.vue, src/views/BinderView.vue</files>
  <read_first>
    - src/components/layout/AppHeader.vue (full file — lines 43-60 for goToFaq + restartTour, lines 260-270 desktop FAQ button, lines 342-348 mobile FAQ button)
    - src/components/layout/MatchNotificationsDropdown.vue (full file — lines 94-103 goToMatches/goToMatch, lines 156 + 202 template call sites)
    - src/components/collection/AddCardModal.vue (lines 420-429 advanced search button)
    - src/components/ui/CardFilterBar.vue (lines 118-123 advanced search button — emits to parent)
    - src/views/CollectionView.vue (lines 690-700 — `handleOpenAdvancedSearch` at line 692-694; also grep for `@open-advanced-search` binding on `<CardFilterBar>` in the template section)
    - src/views/DeckView.vue (lines 280-290 — `handleOpenAdvancedSearch` at line 282-284; also grep for `@open-advanced-search` binding)
    - src/views/BinderView.vue (lines 150-160 — `handleOpenAdvancedSearch` at line 154-156; also grep for `@open-advanced-search` binding)
    - .planning/phases/04-globalsearch-navigation-polish/04-CONTEXT.md (D-07 categories 3+4 — explicitly lists CollectionView.vue:693, DeckView.vue:283, BinderView.vue:155 as convert sites; D-08 what to LEAVE alone, D-09 corrected)
    - .planning/phases/04-globalsearch-navigation-polish/04-RESEARCH.md (Q8 call-site classification table — all 3 parent handlers classified as user-triggered nav that becomes dead post-CardFilterBar-conversion)
  </read_first>
  <action>
Convert the remaining user-triggered navigation sites. EACH file is independent — can be edited in any order. **After the CardFilterBar conversion, the three parent-view dead handlers MUST be cleaned up in the same task (D-07 + Anti-loop Rule 6 — parallel siblings).**



**CONVERSION 1: AppHeader.vue — FAQ link (2 occurrences: desktop + mobile menu)**

The `goToFaq` function (script line 43-47) does:
```typescript
const goToFaq = () => {
  closeHelpMenu()
  closeMobileMenu()
  void router.push('/faq')
}
```

The template calls it twice: line 263 (desktop help menu) and line 343 (mobile help menu).

Replace BOTH `<button @click="goToFaq">` with `<RouterLink>`:

Desktop (around line 262-268):
```html
<RouterLink
    :to="'/faq'"
    @click="closeHelpMenu(); closeMobileMenu()"
    class="w-full flex items-center gap-3 px-4 py-3 text-small text-silver hover:bg-silver-5 hover:text-neon transition-fast text-left"
>
  <span class="w-5 h-5 rounded-full border-2 border-current flex items-center justify-center text-xs font-bold flex-shrink-0">?</span>
  {{ t('help.menu.faq') }}
</RouterLink>
```

Mobile (around line 342-348):
```html
<RouterLink
    :to="'/faq'"
    @click="closeHelpMenu(); closeMobileMenu()"
    class="w-full flex items-center gap-3 px-4 py-3 text-small text-silver hover:bg-silver-5 hover:text-neon transition-fast text-left"
>
  <span class="w-5 h-5 rounded-full border-2 border-current flex items-center justify-center text-xs font-bold flex-shrink-0">?</span>
  {{ t('help.menu.faq') }}
</RouterLink>
```

**IMPORTANT:** Do NOT use `.prevent` (D-09 corrected).

After both template conversions, if `goToFaq` is no longer referenced (grep the file), delete the function definition. Import check: RouterLink already available via Vue Router globally, or import `import { RouterLink } from 'vue-router'` if not implicitly registered — check the existing AppHeader imports.

**DO NOT TOUCH:**
- `restartTour` (line 49-60) — imperative (post-async tour start flow). Stays as is. Per D-08.
- `AppHeader.vue:167/169` — modal-open intents with addCard query. Stay as `router.push`. Per D-08.



**CONVERSION 2: MatchNotificationsDropdown.vue — match notifications (2 occurrences)**

Current `goToMatches` and `goToMatch` (line 94-103):
```typescript
const goToMatches = () => {
  closeDropdown()
  void router.push('/saved-matches')
}

const goToMatch = (match: SimpleMatch) => {
  closeDropdown()
  const matchId = match.docId ?? match.id
  void router.push({ path: '/saved-matches', query: { match: matchId } })
}
```

Template at line 156 calls `goToMatch(match)` inside a `v-for`. Replace with RouterLink; the `:to=` value needs `match.docId ?? match.id`:

Read around line 150-170 first to confirm structure, then replace the clickable element:
```html
<RouterLink
    v-for="match in notifications"
    :key="match.id"
    :to="{ path: '/saved-matches', query: { match: match.docId ?? match.id } }"
    @click="closeDropdown()"
    class="<existing classes>"
>
  <!-- existing match notification row content -->
</RouterLink>
```
(Match the exact classes and content from the current `<button>` or `<div>` element at line 156.)

Template at line 202 `@click="goToMatches"` — this is the "See all matches" footer button. Replace:
```html
<RouterLink
    :to="'/saved-matches'"
    @click="closeDropdown()"
    class="<existing classes>"
>
  {{ t('matches.notifications.goToMatches') }}
</RouterLink>
```

After conversion, delete `goToMatches` and `goToMatch` function definitions if no longer referenced.



**CONVERSION 3: AddCardModal.vue — advanced search link (line 420-429)**

Current:
```html
<button
    @click="handleClose(); router.push('/search')"
    class="text-tiny text-silver-50 hover:text-neon transition-colors whitespace-nowrap"
>
  {{ t('common.actions.advancedSearch') }} →
</button>
```

Replace:
```html
<RouterLink
    :to="'/search'"
    @click="handleClose()"
    class="text-tiny text-silver-50 hover:text-neon transition-colors whitespace-nowrap"
>
  {{ t('common.actions.advancedSearch') }} →
</RouterLink>
```

Import RouterLink if not already: `import { RouterLink } from 'vue-router'` in the script setup block.



**CONVERSION 4: CardFilterBar.vue — advanced search suggestion link (line 118-123)**

This is the trickiest conversion. Current code:
```html
<button v-if="viewMode === 'collection'" @click="emit('open-advanced-search'); clearSuggestions(); dropdownDismissed = true"
  class="w-full px-3 py-2 text-small text-neon hover:bg-neon-10 border-t border-silver-30 flex items-center gap-1">
  <SvgIcon name="search" size="tiny" />
  {{ t('collection.suggestions.advancedSearch') }} →
</button>
```

The parent (`CollectionView.vue:1002`, `DeckView.vue:982`, `BinderView.vue:689`) listens to `@open-advanced-search="handleOpenAdvancedSearch"` which does `router.push({ path: '/search' })`.

Since the button is rendered IN CardFilterBar.vue's template, the cleanest fix is: convert to RouterLink inside CardFilterBar — the parent's handler becomes a no-op target. BUT the parent's handler might do other work (it doesn't — CONFIRMED by direct file read 2026-04-17: CollectionView.vue:691-694, DeckView.vue:282-284, BinderView.vue:154-156 all just do `void router.push({ path: '/search' })`, nothing else).

Convert the CardFilterBar button to RouterLink:
```html
<RouterLink
  v-if="viewMode === 'collection'"
  :to="'/search'"
  @click="clearSuggestions(); dropdownDismissed = true"
  class="w-full px-3 py-2 text-small text-neon hover:bg-neon-10 border-t border-silver-30 flex items-center gap-1"
>
  <SvgIcon name="search" size="tiny" />
  {{ t('collection.suggestions.advancedSearch') }} →
</RouterLink>
```

Note: dropped the `emit('open-advanced-search')` call — the RouterLink navigates directly now. Import `RouterLink` in CardFilterBar.vue if not already.

**Parent-view cleanup for D-07 sites (CollectionView.vue:693, DeckView.vue:283, BinderView.vue:155) — MANDATORY, not optional:**

The CardFilterBar RouterLink conversion supersedes the `open-advanced-search` emit pattern. After CardFilterBar is converted, grep each of these three parent views for `handleOpenAdvancedSearch` and for `@open-advanced-search`. If the handler is no longer called (emit is gone), remove the handler function AND its event binding on the `<CardFilterBar>` element.

Verify step-by-step:
1. `grep "emit('open-advanced-search')" src/components/ui/CardFilterBar.vue` — MUST return 0 matches after the conversion above. If > 0, track each remaining emit site and decide per-site whether to keep or convert.
2. If step 1 returns 0, proceed to remove in CardFilterBar.vue: the `'open-advanced-search': []` emit declaration in the `defineEmits` block (around line 35).
3. In EACH of CollectionView.vue, DeckView.vue, BinderView.vue:
   a. Grep for `@open-advanced-search=` in the template — locate the `<CardFilterBar>` usage and remove the `@open-advanced-search="handleOpenAdvancedSearch"` binding.
   b. Remove the `handleOpenAdvancedSearch` function definition (CollectionView.vue:691-694, DeckView.vue:282-284, BinderView.vue:154-156). Keep the preceding comment removal consistent (CollectionView has a `// Click on "Advanced search" → navigate to /search` comment above the handler — remove it too).
   c. If `router` is no longer used by anything else in the view's script, double-check that the `useRouter` import and the `const router = useRouter()` line still have OTHER callers (there will be — each of these views has other `router.push` sites per D-08 that must be preserved).

**Expected outcome:** `grep "handleOpenAdvancedSearch" src/views/CollectionView.vue src/views/DeckView.vue src/views/BinderView.vue` returns 0 matches after this task. If any match remains, the executor MUST document in the commit message WHY it stays (e.g., "kept in X.vue because emit still fires from component Y that we don't touch in this phase"). Silent retention is not acceptable.

This is Anti-loop Rule 6 applied: CardFilterBar + the 3 parent views are parallel siblings that must be changed atomically, not "I'll do the views later."



**STEP — Full verification after all conversions:**

1. `npm run test:unit` exits 0 (no test regressions)
2. `npx vite build` exits 0
3. `npm run e2e` exits 0 — **REQUIRED per project MEMORY.md `feedback_e2e_before_push.md` rule: MUST run E2E before pushing to develop, not just unit+build.** This rule is non-negotiable and is derived from a real past incident where E2E would have caught a regression that unit+build did not. If any E2E test fails, triage and fix in this task before requesting user approval in Task 4. Do NOT defer to "human checkpoint covers it" — the checkpoint is additional, not a substitute.

Commit: `feat(04-03): convert FAQ, match notifications, AddCardModal, CardFilterBar to RouterLink + clean up parent handlers (D-07)`
  </action>
  <verify>
    <automated>grep -c "<RouterLink" src/components/layout/AppHeader.vue src/components/layout/MatchNotificationsDropdown.vue src/components/collection/AddCardModal.vue src/components/ui/CardFilterBar.vue && grep "@click.prevent.*RouterLink\|RouterLink.*@click\.prevent" src/components/layout/AppHeader.vue src/components/layout/MatchNotificationsDropdown.vue src/components/collection/AddCardModal.vue src/components/ui/CardFilterBar.vue ; grep "handleOpenAdvancedSearch" src/views/CollectionView.vue src/views/DeckView.vue src/views/BinderView.vue ; npm run test:unit && npx vite build && npm run e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<RouterLink" src/components/layout/AppHeader.vue` returns at least 2 (desktop + mobile FAQ — existing /terms and /privacy RouterLinks may also be present, total ≥ 4)
    - `grep "to=\"/faq\"\|:to=\"'/faq'\"" src/components/layout/AppHeader.vue` returns exactly 2 matches
    - `grep -c "<RouterLink" src/components/layout/MatchNotificationsDropdown.vue` returns at least 2
    - `grep "'/saved-matches'" src/components/layout/MatchNotificationsDropdown.vue` returns at least 1 match
    - `grep "<RouterLink" src/components/collection/AddCardModal.vue` returns at least 1 match
    - `grep "<RouterLink" src/components/ui/CardFilterBar.vue` returns at least 1 match
    - `grep "'/search'" src/components/ui/CardFilterBar.vue` returns at least 1 match
    - `grep "@click\.prevent" src/components/layout/AppHeader.vue src/components/layout/MatchNotificationsDropdown.vue src/components/collection/AddCardModal.vue src/components/ui/CardFilterBar.vue src/components/ui/GlobalSearch.vue src/components/ui/MobileSearchOverlay.vue` — every line returned must NOT have RouterLink on the same or adjacent element (verify visually; automated check: `grep -B2 -A0 "@click\.prevent" <files> | grep -i "RouterLink"` returns 0)
    - `grep -E "goToFaq\s*=" src/components/layout/AppHeader.vue` — if this returns 0 matches, the function was removed (acceptable); if it returns 1, the function is retained (only acceptable if it's still called elsewhere — grep call sites)
    - `grep "goToFaq" src/components/layout/AppHeader.vue` returned matches equal to declaration count (no orphan callers)
    - `grep -E "goToMatches\s*=|goToMatch\s*=" src/components/layout/MatchNotificationsDropdown.vue` — if 0, functions were removed cleanly; if non-zero, verify they are still referenced somewhere
    - **D-07 parent-view cleanup (REQUIRED):** `grep "handleOpenAdvancedSearch" src/views/CollectionView.vue src/views/DeckView.vue src/views/BinderView.vue` returns 0 matches. If any match remains, it must be accompanied by an inline comment explaining why (`// Kept: still called from <component>`) AND the commit message must document the exception.
    - **D-07 parent-view cleanup (REQUIRED):** `grep "@open-advanced-search" src/views/CollectionView.vue src/views/DeckView.vue src/views/BinderView.vue` returns 0 matches (all bindings removed from `<CardFilterBar>` usages).
    - **D-07 emit cleanup:** `grep "emit('open-advanced-search')" src/components/ui/CardFilterBar.vue` returns 0 matches.
    - **D-07 emit declaration cleanup:** `grep "'open-advanced-search'" src/components/ui/CardFilterBar.vue` returns 0 matches (declaration in defineEmits also removed).
    - `grep "router.push.*'/search'" src/views/CollectionView.vue src/views/DeckView.vue src/views/BinderView.vue` returns 0 matches on `/search` targets specifically (post-search imperative redirects to OTHER paths, if any, stay per D-08).
    - `grep "SearchView" src/views/SearchView.vue` and verify SearchView.vue:38 STILL has `router.push` (NOT a RouterLink) per D-08 scope exclusion
    - `grep "DecksView" src/views/DecksView.vue && grep -c "router.push" src/views/DecksView.vue` — at least 5 `router.push` sites remain (per D-08, imperative redirects stay)
    - `grep "UserPopover" src/components/ui/UserPopover.vue && grep "router.push('/login')" src/components/ui/UserPopover.vue` returns 1 match (per D-08, post-logout imperative stays)
    - `npm run test:unit` exits 0
    - `npx vite build` exits 0
    - `npm run e2e` exits 0 (REQUIRED per project MEMORY.md feedback_e2e_before_push.md rule — not optional)
    - Git commit exists with `feat(04-03): convert FAQ, match notifications, AddCardModal, CardFilterBar to RouterLink + clean up parent handlers (D-07)`
  </acceptance_criteria>
  <done>All 15 user-triggered navigation sites across the 8 target files use RouterLink. The 3 D-07 parent-view dead handlers (CollectionView.vue, DeckView.vue, BinderView.vue) + their event bindings + the CardFilterBar emit declaration are removed. The 7 imperative-redirect sites (per D-08) explicitly still use router.push. No `.prevent` on any RouterLink with a `:to=` in the entire codebase scope. Build + unit tests + E2E all green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of RouterLink Cmd+click behavior across all conversion sites</name>
  <files></files>
  <action>
Present the summary below to the user and request verification on the dev environment. Wait for approved before closing the phase.

**What was built (automated in Tasks 1-3):**
- 15 user-triggered navigation buttons across 8 files converted to <RouterLink>
- Cmd+click (Mac) / Ctrl+click (Windows) / middle-click opens destinations in a new tab
- Normal left-click navigates as before AND runs side effects (close dropdown, emit close, clear query)
- 7 new i18n keys live in en/es/pt with translations
- MobileSearchOverlay clear button uses the new header.search.clearAriaLabel key
- Dead goTo* wrappers in MobileSearchOverlay removed if orphaned
- Dead `handleOpenAdvancedSearch` handlers + `@open-advanced-search` bindings removed from CollectionView.vue, DeckView.vue, BinderView.vue (D-07 parent-view cleanup)
- Dead `open-advanced-search` emit declaration removed from CardFilterBar.vue

**Verification steps the user must run:**

1. npm run dev — open http://localhost:5173
2. Cmd+click (Mac) / Ctrl+click (Windows) tests — each should open a NEW TAB AND run the side effect on current tab:
   a. GlobalSearch collection result — type bolt, Cmd+click first collection row — new tab at /collection?search=Bolt; dropdown closes + input clears
   b. GlobalSearch user result — switch to users tab, Cmd+click a user — new tab at /@username
   c. GlobalSearch scryfall result — Cmd+click — new tab at /collection?addCard=Name
   d. GlobalSearch advanced search — Cmd+click Advanced search link — new tab at /search
   e. MobileSearchOverlay — repeat a-d with mobile overlay; overlay closes after each click
   f. AppHeader FAQ — Cmd+click FAQ in help menu — new tab at /faq
   g. MatchNotificationsDropdown — Cmd+click a match notification — new tab at /saved-matches?match=X
   h. MatchNotificationsDropdown footer — Cmd+click See all matches — new tab at /saved-matches
   i. AddCardModal advanced search — Cmd+click Advanced search in modal — new tab at /search
   j. CardFilterBar advanced search suggestion — Cmd+click in collection mode — new tab at /search (verify in CollectionView, DeckView, AND BinderView — the parent handler was removed, RouterLink is now the sole nav mechanism)
3. Normal left-click tests — each navigates in SAME tab AND runs side effect:
   - Repeat a-j with plain left-click. Destination loads + side effect fires
   - Special check: CardFilterBar advanced search in CollectionView/DeckView/BinderView — normal click still navigates (confirms D-07 cleanup did not break the happy path)
4. Middle-click tests: any converted link opens in background tab; side effect fires on current tab
5. Regression tests — imperative redirects STILL work as before:
   - Create a new deck from DecksView — auto-navigate to /decks/<id>/edit (NOT in new tab)
   - Log out from UserPopover — auto-navigate to /login
   - Click restartTour — navigate to /collection then start tour
6. Screen reader — ARIA labels work per locale:
   - Focus clear button — announcement: Clear search (en) / Limpiar búsqueda (es) / Limpar busca (pt)
7. Live region — i18n rendering:
   - 2+ results announces {N} results in collection
   - exactly 1 result announces 1 result in ... (singular key)
8. Build: `npx vite build` exits 0
9. **E2E: `npm run e2e` passes (REQUIRED before push per project MEMORY.md `feedback_e2e_before_push.md` rule — MUST run, not optional). Fix any failures before requesting user approval to commit/push.**

Resume signal: user types approved (closes Phase 04) or lists issues to fix.
  </action>
  <verify>User confirms Cmd+click, middle-click, and normal click behave correctly across all 15 conversion sites; imperative redirects still work; i18n announcements correct per locale; `npm run e2e` passes cleanly.</verify>
  <done>User has verified RouterLink behavior, i18n, and combobox-plus-navigation integration on the dev environment. E2E suite green. Phase 04 approved and ready for version bump and production merge consideration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RouterLink `:to` descriptor → URL | Route object built from user search input (card.name, username) flows into URL query/path; Vue Router handles encoding |
| Browser new-tab behavior | OS-level; when guardEvent defers to browser, no app-layer control |
| i18n placeholder interpolation `{count}`, `{tabName}` | Custom useI18n does string replace; values come from internal counts and authored tab names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | RouterLink :to query params built from card.name (user data from Scryfall/Firestore) | accept | Vue Router URL-encodes descriptor query values; destination views (/collection, /@username) read sanitized route.query; no eval/innerHTML flow |
| T-04-03-02 | Open Redirect | :to always points to internal paths (`/search`, `/faq`, `/saved-matches`, `/collection`, `/@<username>`) | mitigate | No user input builds :to directly; card data comes from trusted stores (Firestore) or verified Scryfall API; username flows from public_cards (owner-gated write) |
| T-04-03-03 | XSS | Route query values rendered in destination component | accept | Destination views use `{{ }}` mustache (Vue auto-escape) and `route.query` parameters, not v-html; no change to those templates from this plan |
| T-04-03-04 | Information Disclosure | Query params appear in browser URL bar and history | accept | Search queries and card names already appeared in URLs via the prior `router.push({ query })` flow; no new disclosure — only navigation mechanism changes |
| T-04-03-05 | Spoofing | i18n keys fall back to key-string if missing at runtime | mitigate | Task 1 adds all 7 keys to all 3 locale files with verification greps; custom useI18n returns key string on miss (acceptable sr-only fallback, not a security issue) |
| T-04-03-06 | Click-jacking on middle-click / Cmd+click | Browser default behavior — RouterLink defers | accept | Standard browser behavior; not app-controllable; the opened tab has origin-matching since all :to targets are internal paths resolved against window.location.origin |
</threat_model>

<verification>
1. All 15 template conversions verified by grep acceptance criteria
2. All 7 i18n keys present in all 3 locale files
3. `grep -B2 -A0 "@click\.prevent" src/components/ui/GlobalSearch.vue src/components/ui/MobileSearchOverlay.vue src/components/layout/AppHeader.vue src/components/layout/MatchNotificationsDropdown.vue src/components/collection/AddCardModal.vue src/components/ui/CardFilterBar.vue | grep -i "RouterLink"` returns 0 matches (D-09 correction)
4. `grep "router.push" src/views/SearchView.vue` — still present per D-08
5. `grep "router.push" src/views/DecksView.vue` — 5+ imperative sites still present per D-08
6. **D-07 parent cleanup:** `grep "handleOpenAdvancedSearch" src/views/CollectionView.vue src/views/DeckView.vue src/views/BinderView.vue` returns 0 matches
7. **D-07 parent cleanup:** `grep "@open-advanced-search" src/views/CollectionView.vue src/views/DeckView.vue src/views/BinderView.vue` returns 0 matches
8. `npm run test:unit` exits 0 — no regressions
9. `npx vite build` exits 0
10. `npm run e2e` exits 0 — REQUIRED before push per project MEMORY.md rule
11. Human verification checkpoint passes (Task 4)
</verification>

<success_criteria>
- Cmd+click / middle-click on any of the 15 converted sites opens destination in a new tab
- Normal left-click navigates AND runs side effect (close dropdown / emit close / clear input)
- Screen reader announces combobox state, option selection, result count, tab name — all in the correct language per current locale
- Zero `.prevent` on RouterLinks with `:to=` (verified by automated grep)
- Imperative redirects preserved per D-08 scope
- i18n JSON valid in all 3 locales
- **D-07 parent-view dead handlers removed from CollectionView.vue, DeckView.vue, and BinderView.vue; CardFilterBar emit declaration removed**
- Build passes, unit tests green, E2E tests green (required before push)
- Human verification checkpoint approves
- Phase 4 requirements ARCH-10 and NICE-11 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-globalsearch-navigation-polish/04-03-SUMMARY.md` documenting:
- Template conversion count per file (should sum to 15)
- i18n keys added and their translations table
- Any dead code removed: `goTo*` wrappers in MobileSearchOverlay, `goToFaq` in AppHeader, `goToMatches`/`goToMatch` in MatchNotificationsDropdown, `open-advanced-search` emit in CardFilterBar + `handleOpenAdvancedSearch` in CollectionView/DeckView/BinderView + `@open-advanced-search` bindings on `<CardFilterBar>` in those 3 parent views (D-07 parent-view cleanup)
- Sites explicitly LEFT as router.push per D-08 (SearchView:38, DecksView:331/335/388/487/552, CollectionView:627/629/659, UserPopover:221, AppHeader:57/167/169)
- E2E results summary (test count, pass/fail breakdown) — REQUIRED per project MEMORY.md rule
- Checkpoint verification screenshots / video of Cmd+click behavior
- Commit hashes (3 total: i18n, GlobalSearch+Mobile, remaining-sites-plus-parent-cleanup)

Then:
- Update `.planning/STATE.md`: mark Phase 04 complete, update progress counters
- Update `.planning/ROADMAP.md`: check off Phase 4 box and its 3 plan entries
- Bump `package.json` version: MINOR per CLAUDE.md rule (new feature surface — ARIA combobox + RouterLink navigation)
- Commit: `chore(release): bump version to X.Y.0 (Phase 04 complete)`
</output>
