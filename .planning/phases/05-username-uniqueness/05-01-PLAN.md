---
phase: 05-username-uniqueness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/username.ts
  - tests/unit/utils/username.test.ts
autonomous: true
requirements:
  - UNIQ-01
requirements_addressed:
  - UNIQ-01
must_haves:
  truths:
    - "normalizeUsername('  Rafael_M ') returns 'rafael_m' (trim + lowercase)"
    - "normalizeUsername preserves underscores and digits, only trims and lowercases"
    - "isValidUsername('ab') returns false (too short, < 3 chars)"
    - "isValidUsername('rafael_m') returns true (3-20 word chars)"
    - "isValidUsername('a'.repeat(21)) returns false (too long, > 20 chars)"
    - "isValidUsername('bad name') returns false (space is not a word char)"
    - "isValidUsername trims before testing so '  abc  ' is valid"
  artifacts:
    - path: "src/utils/username.ts"
      provides: "Pure normalizeUsername + isValidUsername functions"
      contains: "export function normalizeUsername, export function isValidUsername"
    - path: "tests/unit/utils/username.test.ts"
      provides: "Unit test coverage for both pure functions (RED before GREEN)"
      contains: ["describe('normalizeUsername')", "describe('isValidUsername')"]
  key_links:
    - from: "src/services/userLookup.ts (Plan 04)"
      to: "normalizeUsername"
      via: "import from utils/username"
      pattern: "import.*normalizeUsername.*utils/username"
    - from: "src/stores/auth.ts (Plan 03)"
      to: "normalizeUsername + isValidUsername"
      via: "import from utils/username"
      pattern: "import.*\\{.*normalizeUsername.*isValidUsername.*\\}.*utils/username"
---

<objective>
Create the pure username normalization + validation utilities that every other plan in this phase depends on (UNIQ-01). `normalizeUsername` and `isValidUsername` are the single source of truth for "what the canonical stored username is" (D-04, D-05). TDD-first: failing tests before any implementation, per CLAUDE.md.

Purpose: Centralize username case/format rules in one tested module so register, loginWithGoogle, changeUsername, checkUsernameAvailable, resolveUsernameToUid, and the migration scripts all agree. This eliminates the case-mismatch bug where `@rafael_m` failed to resolve a stored `Rafael_M` (D-05). These are leaf pure functions — perfect TDD candidates with no Firebase, no Vue.

Output: `src/utils/username.ts` exporting two pure functions; `tests/unit/utils/username.test.ts` covering normalization, length bounds, and character-class rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-username-uniqueness/05-CONTEXT.md
@CLAUDE.md
@src/utils/formatDate.ts
@tests/unit/helpers/fixtures.ts

<interfaces>
<!-- Target API (D-04). Both functions are pure — no I/O, no Firebase, no Vue. -->
```typescript
// src/utils/username.ts
export function normalizeUsername(raw: string): string;  // raw.trim().toLowerCase()
export function isValidUsername(raw: string): boolean;    // /^\w{3,20}$/.test(raw.trim())
```

<!-- The validation regex MUST match the existing changeUsername regex (auth.ts:400): -->
```typescript
const usernameRegex = /^\w{3,20}$/;
```
<!-- \w === [A-Za-z0-9_]. 3-20 chars inclusive. Test against the TRIMMED value. -->

<!-- Test file uses Vitest globals (describe/it/expect) — NO imports of them (CLAUDE.md). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for normalizeUsername + isValidUsername (RED)</name>
  <files>tests/unit/utils/username.test.ts</files>
  <read_first>
    - .planning/phases/05-username-uniqueness/05-CONTEXT.md (D-04, D-05 — exact function bodies)
    - tests/unit/helpers/fixtures.ts (reference for project test style; no fixture needed but confirm conventions)
    - CLAUDE.md (TDD workflow RED→GREEN; Vitest globals are not imported)
  </read_first>
  <behavior>
    Test file exists at tests/unit/utils/username.test.ts.
    - describe('normalizeUsername'):
      - it('trims and lowercases') → normalizeUsername('  Rafael_M ') === 'rafael_m'
      - it('lowercases ALL CAPS') → normalizeUsername('ABCDEF') === 'abcdef'
      - it('leaves an already-normalized value unchanged') → normalizeUsername('rafael_m') === 'rafael_m'
      - it('preserves digits and underscores') → normalizeUsername('User_99') === 'user_99'
      - it('returns empty string for whitespace-only input') → normalizeUsername('   ') === ''
    - describe('isValidUsername'):
      - it('accepts a 3-char lowercase value') → isValidUsername('abc') === true
      - it('accepts a 20-char value') → isValidUsername('a'.repeat(20)) === true
      - it('accepts digits and underscores') → isValidUsername('rafael_m99') === true
      - it('rejects a 2-char value') → isValidUsername('ab') === false
      - it('rejects a 21-char value') → isValidUsername('a'.repeat(21)) === false
      - it('rejects a value with a space') → isValidUsername('bad name') === false
      - it('rejects a value with a hyphen') → isValidUsername('bad-name') === false
      - it('trims before validating') → isValidUsername('  abc  ') === true
      - it('rejects empty string') → isValidUsername('') === false
    All tests MUST FAIL (module/exports do not exist yet).
  </behavior>
  <action>
Create `tests/unit/utils/username.test.ts`. Do NOT import describe/it/expect (Vitest globals per CLAUDE.md). Import the (not-yet-existing) functions:

```typescript
import { normalizeUsername, isValidUsername } from '@/utils/username';
```

Write the full suite per `<behavior>`. Use exact assertions, e.g.:

```typescript
describe('normalizeUsername', () => {
  it('trims and lowercases', () => {
    expect(normalizeUsername('  Rafael_M ')).toBe('rafael_m');
  });
  // ...
});

describe('isValidUsername', () => {
  it('rejects a 21-char value', () => {
    expect(isValidUsername('a'.repeat(21))).toBe(false);
  });
  // ...
});
```

Do NOT create `src/utils/username.ts` yet. Run the test — it MUST fail (cannot resolve module / functions undefined).

Commit: `test(05-01): add failing tests for normalizeUsername and isValidUsername`
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/utils/username.test.ts 2>&1 | grep -E "FAIL|failed|Cannot find" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/utils/username.test.ts` exists
    - `grep -c "describe(" tests/unit/utils/username.test.ts` returns at least 2
    - `grep -c "it(" tests/unit/utils/username.test.ts` returns at least 13
    - `grep "import.*normalizeUsername.*isValidUsername" tests/unit/utils/username.test.ts` returns at least 1 match
    - `npm run test:unit -- tests/unit/utils/username.test.ts` exits non-zero (RED phase confirmed)
    - Git log shows a commit matching `test(05-01): add failing tests`
  </acceptance_criteria>
  <done>Failing test file committed; tests fail because the module/functions do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/utils/username.ts to pass the tests (GREEN)</name>
  <files>src/utils/username.ts, tests/unit/utils/username.test.ts</files>
  <read_first>
    - tests/unit/utils/username.test.ts (the failing tests from Task 1)
    - .planning/phases/05-username-uniqueness/05-CONTEXT.md (D-04 exact bodies, D-05 canonical-lowercase rule)
    - src/utils/formatDate.ts (reference for the project's plain-export util style)
    - CLAUDE.md (GREEN: minimal code; Rule 7: test:unit + npx vite build = done)
  </read_first>
  <behavior>
    - src/utils/username.ts exists, exports normalizeUsername and isValidUsername.
    - normalizeUsername(raw) === raw.trim().toLowerCase().
    - isValidUsername(raw) === /^\w{3,20}$/.test(raw.trim()).
    - All Task 1 tests pass (GREEN).
    - npx vite build succeeds (no type errors).
  </behavior>
  <action>
Create `src/utils/username.ts` with EXACTLY these implementations (D-04 verbatim — the regex matches auth.ts:400):

```typescript
/**
 * Canonical username normalization (D-04, D-05).
 * The stored `username` field is ALWAYS this normalized value everywhere.
 */
export function normalizeUsername(raw: string): string {
  return raw.trim().toLowerCase();
}

/**
 * Username format validation (D-04).
 * Mirrors the existing changeUsername regex (auth.ts:400): /^\w{3,20}$/.
 * \w === [A-Za-z0-9_]; 3-20 chars inclusive. Tested against the trimmed value.
 */
export function isValidUsername(raw: string): boolean {
  return /^\w{3,20}$/.test(raw.trim());
}
```

Run `npm run test:unit -- tests/unit/utils/username.test.ts` — all tests MUST pass.
Run `npx vite build` to confirm no type errors (Rule 7).

Commit: `feat(05-01): add normalizeUsername and isValidUsername pure utils`
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/utils/username.test.ts && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `src/utils/username.ts` exists
    - `grep "export function normalizeUsername" src/utils/username.ts` returns 1 match
    - `grep "export function isValidUsername" src/utils/username.ts` returns 1 match
    - `grep -F "raw.trim().toLowerCase()" src/utils/username.ts` returns 1 match
    - `grep -F "/^\\w{3,20}$/" src/utils/username.ts` returns 1 match
    - `npm run test:unit -- tests/unit/utils/username.test.ts` exits 0 (all green)
    - `npx vite build` exits 0
    - Git log shows both `test(05-01)` and `feat(05-01)` commits (atomic RED → GREEN)
  </acceptance_criteria>
  <done>Both pure functions implemented; all unit tests green; build succeeds; RED→GREEN commits in git log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | These are pure functions with no I/O, no network, no Firebase, no DOM. Inputs are already-typed strings. No trust boundary is crossed within this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | isValidUsername regex used as a security gate downstream | mitigate | Anchored regex `/^\w{3,20}$/` (`^`/`$`) rejects any embedded whitespace/control/path chars; \w excludes `/`, `.`, `$` so a normalized value is always a safe Firestore doc ID in Plan 02 |
| T-05-01-02 | Denial of Service | normalizeUsername on a huge string | accept | Linear-time trim/toLowerCase; callers (register/changeUsername) bound input via form maxlength + isValidUsername (≤20) before persistence; no catastrophic-backtracking regex |
</threat_model>

<verification>
1. `npm run test:unit -- tests/unit/utils/username.test.ts` exits 0 — all cases green
2. `npm run test:unit` (full suite) exits 0 — no regressions
3. `npx vite build` exits 0 — types resolve
4. `grep -F "/^\\w{3,20}$/" src/utils/username.ts` returns 1 (regex matches changeUsername source-of-truth)
</verification>

<success_criteria>
- `src/utils/username.ts` exports the two pure functions with the exact D-04 bodies
- Unit test file has ≥2 describe blocks and ≥13 it cases, all green
- Build succeeds
- Two atomic commits (RED then GREEN) in git log
- normalizeUsername is the lowercase-trim canonical form (D-05); isValidUsername regex is identical to auth.ts:400
</success_criteria>

<output>
After completion, create `.planning/phases/05-username-uniqueness/05-01-SUMMARY.md` documenting:
- Exact function signatures exported
- Test file location + `npm run test:unit:coverage` percent for src/utils/username.ts (target 100% — trivial pure functions)
- Confirmation the regex matches auth.ts:400
- Commit hashes for RED/GREEN commits
- Note for Plan 03 / Plan 04 / Plan 05: import these from `@/utils/username`
</output>
