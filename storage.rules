rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // card-images/ is served exclusively through the cardImage Cloud Function
    // (admin SDK, bypasses these rules). No direct client read/write.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
