{
  "key": "TASK-129",
  "title": "Perf F2: destrabar el gate de auth del router para rutas guest — pintar sin esperar el round-trip de Firebase Auth",
  "description": "Parte del loop perf 2026-07-17 (goal: APPRENDER FAST3G ≤1066ms, baseline 2132). Causa raíz principal: router.beforeEach (src/router/index.ts:166-183) bloquea TODA primera navegación esperando authStore.loading en un while con re-esperas de 2s — incluso rutas guest (/login, estáticas) esperan el round-trip a identitytoolkit antes del primer render. Fix: (a) para rutas SIN requiresAuth ni requiresGuest, resolver la navegación sin esperar auth; (b) para requiresGuest (/login), render optimista con último-estado-conocido cacheado en localStorage (si el last-known dice logueado, esperar auth o redirigir optimista a /saved-matches; si dice guest o no hay dato, pintar /login ya y redirigir DESPUÉS si auth resuelve logueado — sin flicker para el caso común); (c) requiresAuth mantiene la espera actual (no degradar seguridad de UX — la data igual está protegida por reglas Firestore). CUIDADO: no romper returnUrl, no crear loops de redirect, no tocar initAuth (Regla: cambio mínimo). Tests del guard con auth lenta/rápida/error.",
  "acceptance_criteria": [
    "Rutas guest (login y estáticas) renderizan sin esperar la resolución de Firebase Auth (test con auth artificialmente lenta: la vista aparece antes del resolve)",
    "Usuario logueado que entra a /login sigue terminando en /saved-matches (redirect post-resolve u optimista por last-known), returnUrl intacto, sin loops",
    "requiresAuth se comporta igual que hoy; suite E2E completa verde",
    "APPRENDER FAST3G medido con el harness ≤ 1400ms tras este ticket (aporte parcial al goal ≤10 66)",
    "Gates verdes"
  ],
  "status": "done",
  "priority": "medium",
  "labels": [
    "perf",
    "router",
    "auth",
    "guest-load",
    "v1.52"
  ],
  "assignee": null,
  "depends_on": [
    "TASK-128"
  ],
  "linked_commits": [
    "eedc0bb",
    "114a69c",
    "e753a33"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-07-17T16:05:00.000Z",
      "body": "2026-07-17 orquestador: CERRADO tras ciclo review completo (rev-129: REQUEST_CHANGES → fix → APPROVE). TDD real (25 tests RED primero + 9 del fix batch = 1408 unit). Implementado: guard extraído a src/router/authGuard.ts (factory inyectable) — (a) rutas sin flags resuelven sin esperar auth, (b) requiresGuest optimista con last-known en localStorage ('ct_auth_last_known', solo strings 'authenticated'/'guest'), (c) requiresAuth byte-idéntico (verificado por diff contra código viejo). App.vue era un SEGUNDO gate (v-if authStore.loading tapaba todo) — unificado vía shouldBlockOnAuthLoading compartida. HIGH del review (loader inalcanzable por META vacío de START_LOCATION → shell en blanco en entradas requiresAuth frías) y MEDIUM (redirect diferido secuestraba navegación posterior del usuario) arreglados en e753a33 con tests de regresión c/u. VERIFICADO: E2E completa 2 veces (125 passed pre-fix, passed limpio post-fix), harness por orquestador independiente: FCP 896 / APPRENDER 2574 FAST3G sin drift. RESIDUAL LOW documentado: si auth resuelve ANTES de que confirme la navegación a /login (chunk lento), el redirect legítimo se salta y el usuario autenticado ve /login una vez (fail-safe, sin loop; el próximo cold load redirige bien) — recordar si llega un reporte así. AC4 (APPRENDER ≤1400) FALLÓ honestamente: 2545→2574 sin movimiento — causa raíz: chunk firebase 486KB/115KB gz eager en toda ruta (la espera de auth estaba escondida detrás de la descarga); target transferido a TASK-132."
    }
  ],
  "created_at": "2026-07-17T13:42:17.833Z",
  "updated_at": "2026-07-17T16:05:00.000Z",
  "jira_key": null,
  "verification_tier": "tdd",
  "marker": "[EXPLICIT]"
}
